
// This file is deprecated and no longer used.
// VAPID key generation is now handled directly on the client-side
// in the VapidKeyGenerator component to prevent bundling server-side modules
// with client code, which was causing build failures.
'use server';
