rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This is a temporary rule to unblock development. It allows any logged-in
    // user to read and write all data.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
